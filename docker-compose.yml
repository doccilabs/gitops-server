services:
  gitops-server:
    build:
      context: .
      dockerfile: Dockerfile
    image: gitops-server:latest
    ports:
      - "3000:3000"
    environment:
      NODE_ENV: production
      PORT: 3000
    restart: unless-stopped